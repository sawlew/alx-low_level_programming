#include "main.h"

/**
 * _puts - a function that prints a string to the stdout
 * @str: strings to print
 *
 * Return: the string
 */

void _puts(char *str)
{
char *str;


_puts(str);

_puts('\n');
}
